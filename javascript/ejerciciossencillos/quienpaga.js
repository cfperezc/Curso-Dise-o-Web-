function whosPaying(names) {

        var tamaño=names.length;
        var gente= names;
        var aleatorio=Math.floor(Math.random()*tamaño); 
    
        return gente[aleatorio] + " is going to buy launch today";
    }
    
    var amigos=["alexa","carlos","fabian","manuela","martina"];